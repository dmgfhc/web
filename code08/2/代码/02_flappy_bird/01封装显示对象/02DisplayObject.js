/**
 * 一个标准的显示对象
 */
function DisplayObject() {
    // 显示对象本身的数据
}

// 显示对象所需的绘制函数
DisplayObject.prototype.draw = function () {
    // 根据显示对象的数据去绘制显示对象
};

// 显示对象的更新函数
DisplayObject.prototype.update = function(){
    // 根据时间的变化，来更新显示对象的数据
};